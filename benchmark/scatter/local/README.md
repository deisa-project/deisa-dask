
From `benchmark/scatter/local` directory, run the following command to launch the benchmark in development mode:
```shell
PYTHONPATH=<PATH_TO_CODE>/deisa-project/deisa-dask/src:$PYTHONPATH ./launch-local.sh
```
